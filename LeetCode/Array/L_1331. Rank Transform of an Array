//TimeOut

class Solution {
    public int[] arrayRankTransform(int[] arr) {
        TreeSet<Integer> ts = new TreeSet<>();
      for(int i:arr){
        ts.add(i);
      }
      int res[] = new int[arr.length];
      for(int i=0;i<arr.length;i++){
        res[i] = ts.headSet(arr[i]).size()+1;
      }
      return res;
    }
}
